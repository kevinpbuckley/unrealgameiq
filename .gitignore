# Game IQ generated index (gitignored inside target projects too)
.gameiq/

# UE plugin build artifacts (when plugin/GameIQ is compiled in a project)
plugin/**/Binaries/
plugin/**/Intermediate/
plugin/**/Saved/
plugin/**/DerivedDataCache/

# OS / editor
.DS_Store
.vs/
.idea/
*.swp
chat.log
chat2.log
*.log
